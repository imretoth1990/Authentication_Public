import React, { useState } from "react";
import { getAllUsers } from "../../client/buttons.client";

export function findUsername(inputValue, usersData, setMessage) {
  /**
   * compare inputValue to existing objects (query)
   * where().exists()
   *  */
  /**
   * if inputValue exists:
   * setMessage("Existing ..., please choose an other one")
   *
   * else:
   *
   * setClassName()
   */
}

export function findEmail(inputValue, usersData, setMessage) {
  /**
   * compare inputValue to existing objects (query)
   * where().exists()
   *  */
  /**
   * if inputValue exists:
   * setMessage("Existing ..., please choose an other one")
   *
   * else:
   *
   * setClassName()
   */
}
